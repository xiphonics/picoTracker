-include $(PWD)/base_rules

#---------------------------------------------------------------------------------
%.app: $(OFILES)
	$(CXX) $(LDFLAGS) -arch $(shell uname -m) -o $@ $(OFILES) $(LIBS) $(shell pkg-config --cflags --libs sdl2)
	mv $@ ../lgpt
