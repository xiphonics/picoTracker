-include $(PWD)/base_rules

#---------------------------------------------------------------------------------
%.app: $(OFILES)
	$(CXX) $(LDFLAGS) -o $@ $(OFILES) $(LIBS) $(shell pkg-config --cflags --libs sdl2)
	mv $@ ../lgpt
